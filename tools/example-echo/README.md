# example-echo

Minimal AI SDK 6-first tool used as a contract reference inside the private toolshed monorepo.

## Usage

```ts
import { exampleEchoTool } from "./src/index"
```

This example is not mapped for publishing by default. Add an entry in `packages/tools/tool-release-map.json` before tagging releases.

